content = "<%= j render 'booking_history', booking_history: @booking_history %>"

vex.open({
  content: content,
  unsafeContent: '',
  showCloseButton: false,
  escapeButtonCloses: false,
  overlayClosesOnClick: true,
  appendLocation: 'body'
});
