var issues_list = $('.issue-container[data-issueable-id=' + "<%= j raw @issueable.id %>" +']').find('.issues-list');
var popover = $('.edit-issue-link[data-issue-id=' + "<%= j raw @measurement_issue.id %>" +']');
html = "<%= j render '/measurement_issues/issues_list', issues: @issueable.measurement_issues, issueable: @issueable %>"

issues_list.html(html);

$.measurement_issues.resolve_submit_buttons_state();

popover.popover('dispose');
