if ($("#comment-form").length == 1) {
  $("#fakeloader").fakeLoader({
    spinner: 'spinner1'
  });
  $("#comment-form").submit()
}
else
  window.location.href = "<%= j customer_path(@profile.customer.id) %>"